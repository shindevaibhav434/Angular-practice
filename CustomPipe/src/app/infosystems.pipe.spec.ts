import { InfosystemsPipe } from './infosystems.pipe';

describe('InfosystemsPipe', () => {
  it('create an instance', () => {
    const pipe = new InfosystemsPipe();
    expect(pipe).toBeTruthy();
  });
});
